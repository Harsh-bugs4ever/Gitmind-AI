# GitMind — AI services requirements
# Run: pip install -r ai/requirements.txt
# Also add these to backend/pyproject.toml dependencies section

sentence-transformers>=3.0.0
google-generativeai>=0.5.0
numpy>=1.26.0
